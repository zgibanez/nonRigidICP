function computeBilinearWeights(ab,bins)
%Inputs:
% ab: - alpha-beta mapping
% bins: number of cells for voting algorithm

% The equations relating spin-map coordinates (?,?) and spin-image bin (i,j) are
ij = 
end